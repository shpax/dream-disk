const awilix = require('./awilix.default');
const web = require('./web.default');
const rest = require('./rest.default');
const memory = require('./memory.default');

module.exports = {
  awilix,
  web,
  rest,
  memory
};